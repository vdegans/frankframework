<ApiListener
	name="{{params.description.root.name}}"
	method="{{payload.root.method}}"
	uriPattern="{{payload.root.uriPattern}}"
	headerParams="{{payload.root.headerParams}}"
	produces="{{payload.root.produces}}"
	consumes="{{payload.root.consumes}}"
/>
